# The smallest bundle: one binary. Used by `make quickstart` to show the bundling
# and deploy-script generation steps without needing a real deployment.

BUNDLE_NAME=example-hello
BUNDLE_DESC="Just the hello-world binary (used by make quickstart)"

BUNDLE_COMPONENTS=hello-world
BUNDLE_STRIP=no
