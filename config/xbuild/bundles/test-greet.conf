# Test fixture: a bundle with an executable and the library it needs, to prove that
# RPATH=$ORIGIN makes the deployed directory self-contained (no LD_LIBRARY_PATH).
BUNDLE_NAME=test-greet
BUNDLE_DESC="Test fixture: app + its sibling library, self-contained via ORIGIN"
BUNDLE_COMPONENTS=test-greet-app test-greet-lib
BUNDLE_STRIP=no
