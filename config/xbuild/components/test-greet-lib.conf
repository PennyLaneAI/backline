# Test fixture: a shared library with no external dependencies.
# Exercises COMPONENT_KIND=shared-library and SONAME handling.
COMPONENT_NAME=test-greet-lib
COMPONENT_DESC="Test fixture: shared library (dependency target for test-greet-app)"
COMPONENT_KIND=shared-library
COMPONENT_OUTPUT=libgreet.so
COMPONENT_SOURCES=$(ROOT_DIR)/examples/hello-world/src/greet.cpp
